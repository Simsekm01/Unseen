# Optional. Wenn leer -> App laeuft im LOKALEN Modus (Fotos in IndexedDB, nur auf diesem Geraet).
# Sobald beide Werte gesetzt sind, nutzt die App Supabase als Backend (Fotos zentral, fuer alle Gaeste sichtbar).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Event-ID in der Supabase 'events' Tabelle (siehe supabase-schema.sql)
VITE_EVENT_ID=kaycee-mustafa
